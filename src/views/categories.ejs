<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <h2>Available Categories</h2>
        <ul>
            <% categories.forEach((category)=> { %>
                <li>
                    <strong>
                        <%= category.name %>
                    </strong>
                </li>
                <% }); %>
        </ul>
    </main>
    <%- include('partials/footer') %>