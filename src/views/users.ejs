<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <ul>
            <% console.log(users) %>
                <% users.forEach((user)=> { %>
                    <li>
                        <strong>
                            <%= user.name %>
                        </strong>: <%= user.email %> --- Role: <%= user.role_name %>
                    </li>
                    <% }); %>
        </ul>

    </main>
    <%- include('partials/footer') %>